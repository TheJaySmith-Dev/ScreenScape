{
  "name": "watchnow-backend",
  "version": "1.0.0",
  "description": "Backend services for WatchNow",
  "scripts": {
    "build": "echo \"No build required for this project. Netlify handles function compilation automatically.\"",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.3",
    "drizzle-orm": "^0.31.2",
    "jose": "^5.6.2"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.6",
    "drizzle-kit": "^0.22.7",
    "typescript": "^5.4.5"
  }
}